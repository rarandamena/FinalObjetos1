import java.util.UUID;

/**
 * la clase coche hace referencia a un unico coche por usuario. Es tambien al cual se somete a reparaciones dentro del taller
 */

public class Coche {
    /**
     * el UUID se refiere a que un coche solo puede tener un identificador universal
     */
    private UUID Serial;





}
